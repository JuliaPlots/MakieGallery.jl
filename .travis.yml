language: julia

os:
  - linux

julia:
  - 1.2

# # Uncomment the following lines to allow failures on nightly julia
# # (tests will run but not make your overall status red)
# matrix:
#   allow_failures:
#   - julia: nightly

notifications:
  email: false

script: # clone the reference images, don't actually test anything - nothing will work since Travis doesn't have GPUs
  - echo "We don't actually test anything here - nothing will work, since Travis doesn't have GPUs"
  - julia --color=yes -e 'printstyled("TODO: ", color = 1, bold = true); print("add documentation pushing")'

# after_success:
#  - julia -e 'if VERSION >= v"0.7.0-" using Pkg end; cd(Pkg.dir("Example")); Pkg.add("Coverage"); using Coverage; Coveralls.submit(Coveralls.process_folder())';
#  - julia -e 'if VERSION >= v"0.7.0-" using Pkg end; cd(Pkg.dir("Example")); Pkg.add("Coverage"); using Coverage; Codecov.submit(Codecov.process_folder())';

# jobs:
#   include:
#     - stage: "Documentation"
#       julia: 1.0
#       os: linux
#       script:
#         - pushd ~
#         - git clone --depth=1 https://github.com/SimonDanisch/ReferenceImages
#         - popd
#         - julia --project=docs/ -e 'using Pkg; Pkg.instantiate();
#                                     Pkg.develop(PackageSpec(path=pwd()))'
#         - julia --project=docs/ docs/make.jl
#       after_success: skip
